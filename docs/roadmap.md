# GRID Roadmap

## Phase 1 — Graph model

- define infrastructure node types
- define dependency edge types
- define observation semantics

## Phase 2 — Topology

- build graph ingestion and normalization
- support upstream/downstream queries
- expose impact paths

## Phase 3 — Ecosystem integration

- consume discovery from GHOST
- provide context to PULSE and TRACE
- support BLACKBOX and RECOVER investigations